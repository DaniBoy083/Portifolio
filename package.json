{
  "name": "portifolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/generate-build-meta.cjs",
    "build": "tsc",
    "watch": "npm run prebuild && tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
